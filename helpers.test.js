describe('Helpers test(with set-up and tear-down)', function(){
beforeEach(function(){
billAmt=100;
tipAmt=20;
};
})

it('should get the tip amount from the whole bill', function(){
expect('tipAmt').toEqual(20)


billAmtInput=500;
tipAmtInput=100;
submitPaymentInfo
});





it('should get the total for the whole bill', function(){
total=0
expect(tipAmt.toEqual(20))
expect(billAmt=100)
submitPaymentInfo();
expect(billAmt+tipAmt=total)
});

it('should get the percentage of the tip', function(){
expect ('tipPercent').toEqual(20)
billAmtInput=500;
tipAmtInput=100;
});

it('should convert the bill and tip amount into a tip percent function', function(){
expect (calculateTipPercent(100,25)).toEqual(25);
});

it('should generate a new td from value and append to tr'(tr,value), function(){
    expect
});